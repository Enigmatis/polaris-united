{
  "request": "mutation($title:String!, $creationTime: String!, $authorId:String) { createBookWithCreationDate(title:$title,creationTime: $creationTime,authorId:$authorId) { id title creationTime } }"
}
