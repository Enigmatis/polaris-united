{
  "request": "mutation($description:String!,$rating:String!, $bookId:String!, $reviewKind:ReviewKind!){\n  createReview(description:$description,rating:$rating, bookId:$bookId, reviewKind:$reviewKind)\n{ id }}"
}
